# Direct dependencies. Lower bound = oldest tested-compatible; upper
# bound = next major (bumps need a manual compat check). For a fully
# reproducible install, run `pip-compile` against this file.
streamlit>=1.28,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<3.0
altair>=5.0,<6.0
streamlit-aggrid>=0.3.4,<2.0
openpyxl>=3.1.2,<4.0
requests>=2.28,<3.0
